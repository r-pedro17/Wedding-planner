---
schema: 3
project: P-9CB17850
id: T-20260904-0002-S47365C2A12052ECD
short_id: T-20260904-0002
priority: normal
initiative:
context_refs: []
evidence_refs: [.tickets/_proofs/T-20260904-0002-S47365C2A12052ECD/T-20260904-0002-F3-audit.md]
project_effect:
created_at: 2026-09-04T23:24:00+02:00
requested_by: human
origin_agent: claude-opus-4-8
origin_run: S47365C2A12052ECD
lock_version: 2
depends_on: []
claim_attempt: 1
claim_agent: claude-opus-4-8
claim_run: S07D7792B90946C03
claimed_at: 2026-09-04T23:31:27+02:00
work_branch: work/T-20260904-0002-S07D7792B90946C03
implementation_sha: 9ab698707fbac3adc392a709f2cd2245daf4ca28
review_agent: convex-reviewer
review_run: SA80DA0B788010174
reviewed_at: 2026-09-05T00:35:43+02:00
review_result: APPROVED
memory: pending
claim_mode: local
---

# F3 durable audit events

## Outcome

Foundation gate F3: immutable, wedding-scoped `auditEvents` are written in the
same Convex transaction as each important successful mutation, with a
server-derived (unforgeable) `source`, and proven by tests. Next gate after
F0-F2, which are shipped on `main`.

## Context

BUILD_PLAN.md:88-110 defines F3. No `auditEvents` table, `convex/lib/audit.ts`
helper, or audit tests exist yet (grep `auditEvents` in `convex/` returns
nothing). Full ready-to-execute spec: `_tmp/HANDOFF-T-20260904-0002.md`
(call sites, schema conventions, auth primitives, the 5 proof points).

## Acceptance criteria

- [x] `auditEvents` table in `convex/schema.ts` (`by_wedding` +
      `by_wedding_and_time`; all labels `v.union(v.literal(...))`).
- [x] Server-only helper inserting inside the caller's mutation transaction;
      `source` derived server-side.
- [x] Important mutations instrumented after write + after the auth guard.
- [x] Wedding-member-gated read query; no public API to alter history.
- [x] `convex-test` specs prove: exactly-once event per important mutation; no
      event on rejected mutation; member-only read; no historical-mutation API;
      UI caller cannot forge `source: "eve"`/`"system"`.
- [x] `SECURITY.md` "Known gaps" audit line updated once tests prove it.

## Constraints

Audit rows exclude content (no prompts, notes, contacts, amounts, tokens,
snapshots) — only ids, controlled labels, actor, source, ts. `source` never a
public mutation arg. Do NOT change existing authz rules,
`tests/fixtures/identities.ts`, or `*-authz.test.ts`. Work on `main`, no
branches. Keep `pnpm verify` green and pass `pnpm fallow:ci`. Do not start F4.

## Unknowns / Proofs

Which mutations count as "important" (creates/updates/deletes/payments/
membership yes; `ensureUser` idempotent upsert likely no) — confirm against
docs/PRODUCT_SPEC / ENGINEERING, not blindly.

## Implementation

- `convex/schema.ts`: `auditEvents` table + closed-vocabulary unions
  (`auditSource` ui|eve|system, `auditActorKind`, `auditAction`, `auditEntity`);
  indexes `by_wedding` and `by_wedding_and_time`. Rows carry only ids +
  controlled labels + actor + source + `at` — no content.
- `convex/lib/audit.ts`: two server-only entry points instead of one
  `recordAuditEvent` — `recordUserEvent` (→ `source: "ui"`, `actorKind: "user"`)
  and `recordSystemEvent` (→ `source: "system"`, `actorKind: "system"`). Neither
  reads `source` from anywhere, so forgery is structurally impossible; `eve`
  stays reserved for F4 and is emitted nowhere yet.
- Instrumented mutations (after auth guard + after the write): tasks
  create/update/complete/remove; budgets addCategory/addItem/updateItem/
  recordPayment/removeItem; guests create/update/remove; vendors add/update/
  remove; weddings create/update/addMember; reminders dismiss (user) and
  refreshAll (system, one refresh event per wedding that gained reminders).
  `ensureUser` (idempotent upsert) and `pruneResolved` (derived-state GC) are
  intentionally NOT audited.
- `convex/audit.ts`: member-gated `list` query (newest-first, bounded); no
  mutation exists to alter history.
- `tests/audit-convex.test.ts`: 6 specs covering all 5 proof points (+ the
  system-source path). 80 tests pass.

## Verification

`pnpm test`, `pnpm typecheck` (run `pnpm exec convex codegen` first), `pnpm
verify`, `pnpm fallow:ci --base $(git rev-parse origin/main)`.

## Handoff

`_tmp/HANDOFF-T-20260904-0002.md` is the execution brief. Requires a Convex
deploy to run against prod — confirm authorization before deploying.

## Review

## Durable learning candidates
