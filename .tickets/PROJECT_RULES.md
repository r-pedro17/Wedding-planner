# Project rules

Project-specific rules may specialize or narrow the baseline contract.
They must not weaken baseline safety, authority, claim, or provenance invariants without an explicit versioned override.
